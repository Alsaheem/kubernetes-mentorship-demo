apiVersion: kustomize.config.k8s.io/v1beta1

kind: Kustomization

metadata:
  namespace: production

bases:
  - ../../kustomize/base

patches:
  - backend-patch.yaml
  - frontend-patch.yaml

resources:
  - secrets.yaml

commonLabels:
  project: devops

namePrefix: vector-
